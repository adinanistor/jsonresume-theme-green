{{#if resume.work.length}}
	<section id="work">
		<h2><strong>Work Experience</strong></h2>
		{{#each resume.work}}
		<div class="item">
			<dl>
				<dt>Dates:</dt>
					<dd>{{startDate}}-{{endDate}}</dd>
				<dt>Position:</dt>
					<dd><strong>{{position}}</strong></dd>
				<dt>Employer:</dt>
					<dd><strong>{{company}}</strong></dd>
				<dt>Main activities:</dt>
					<dd><ul>
						{{#each highlights}}
							<li>{{.}}</li>
						{{/each}}
					</ul></dd>
			</dl>
			<br>
		</div>
		{{/each}}
	</section>
{{/if}}